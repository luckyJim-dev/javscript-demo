const sample = function(number){
    factor = 4;
    if (number==2){
        return number*factor;
    }
    return number*10;
};
console.log(sample(2));
/**
 * install ESLint
 * npm install -g eslint
 * eslint --init
 */